name: Test Google News RSS

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Test Google News RSS
        run: |
          python -c "
import requests, sys, re

companies = [
    'ByteDance', 'TikTok', 'Tencent', 'Alibaba', 'JD.com', 'Kuaishou', 'Ant Group',
    'Meituan', 'Kakao', 'Naver', 'Rakuten', 'Sea Limited', 'Grab', 'Gojek',
    'VNG Group', 'Yahoo', 'Cyberagent', 'Adyen', 'Zalando', 'Allegro', 'Trendyol',
    'MercadoLibre', 'Rappi', 'Noon', 'Careem', 'Tabby', 'Kaspi.kz', 'Jumia', 'Konga',
]

ok, fail = 0, []
for name in companies:
    url = f'https://news.google.com/rss/search?q={requests.utils.quote(name)}&hl=en-US&gl=US&ceid=US:en'
    try:
        r = requests.get(url, timeout=10)
        if r.status_code == 200 and '<rss' in r.text:
            items = re.findall(r'<title>(.*?)</title>', r.text)
            print(f'OK  {name:15s} | {len(items)-1} titles')
            ok += 1
        else:
            print(f'FAIL {name} | HTTP {r.status_code}')
            fail.append(name)
    except Exception as e:
        print(f'FAIL {name} | {type(e).__name__}')
        fail.append(name)

print(f'
=== 结果: {ok}/{len(companies)} 通过 ===')
if fail:
    print(f'失败: {fail}')
    sys.exit(1)
"
